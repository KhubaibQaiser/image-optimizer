const constants = {
  MAX_FILE_SIZE: 50 * 1024 * 1024, // 50MB
  ALLOWED_MIME_TYPES: ['image/jpeg', 'image/jpg', 'image/png', 'image/webp'],
  OUTPUT_QUALITY: 80, // Balanced quality for WebP
};

module.exports = constants;
